[workspace]
resolver = "2"
members = ["engine", "game"]
# `cargo run`/`cargo build` with no `-p` flag operate on the game binary; the
# engine is a library and has nothing to run.
default-members = ["game"]
