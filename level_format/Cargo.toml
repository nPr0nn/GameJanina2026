[package]
name = "level_format"
version = "0.1.0"
edition = "2021"

[dependencies]
# Geometry — Vec2D is glam::Vec2, used in Shape::contains.
glam = { version = "0.33" }
# Level serialization to/from JSON.
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1" }
